# 第一版源代码复制内容清单

更新时间：2026-05-15

## 已复制内容

### 前端源码

目标目录：

```text
N:\mutou\第一版源代码\frontend-source
```

来源：

```text
N:\mutou\xm\xjg
```

包含：

- `package.json`
- `package-lock.json`
- `vite.config.js`
- `index.html`
- `.env.production`
- `src/`
- `mock/`
- `scripts/`
- `docs/`

排除：

- `node_modules/`
- `dist/`
- `output/`
- `test-results/`
- `tmp/`
- `.codex-dev/`
- `.gstack/`
- `.playwright-cli/`
- `*.log`
- `*.out.log`
- `*.err.log`
- `index-approval.html`

### 前端构建产物

目标目录：

```text
N:\mutou\第一版源代码\frontend-dist
```

来源：

```text
N:\mutou\xm\xjg\dist
```

说明：复制前已在 `N:\mutou\xm\xjg` 执行 `npm run build`，构建成功，产物包含 `index.html` 和 `assets/`。

### 后端源码

目标目录：

```text
N:\mutou\第一版源代码\backend-source
```

来源：

```text
N:\mutou\xm\xjg-api
```

包含：

- `package.json`
- `package-lock.json`
- `README.md`
- `.env.example`
- `.env.production.example`
- `src/`
- `scripts/`
- `docs/`
- `test/`

排除：

- `node_modules/`
- 真实 `.env`
- `data/`
- `*.log`
- `*.out.log`
- `*.err.log`
- `run*.log`
- `codex*.log`

后端源码中的 `src/logs/` 目录可能由日志配置在运行时使用，但历史日志文件未复制。生产运行后会按后端日志配置重新生成日志。

### 文档

目标目录：

```text
N:\mutou\第一版源代码\docs
```

已按用途整理为：

- `01-项目概览/`
- `02-技术架构/`
- `03-部署上线/`
- `04-接口文档/`
- `05-排期模块/`

重点文档包括：

- `项目概述.md`
- `项目目录结构.md`
- `已完成功能&已实现接口清单.md`
- `已经实现的功能-2026-05-15.md`
- `前端技术架构&框架规范.md`
- `后端技术架构&框架规范.md`
- `数据库表结构设计.md`
- `正式上线操作手册.md`
- `deploy-frontend.md`
- `deploy-backend.md`
- `deployment-handoff.md`
- `production-mysql-runbook.md`
- `production-readiness-checklist.md`
- `ops-runbook.md`
- `api-workspace.md`
- `auth-api.md`
- `board-api.md`
- `schedule-api.md`
- `frontend-api-contract.md`

## 重要提醒

- 后端真实 `.env` 未复制。上线机器必须基于 `.env.production.example` 单独创建。
- MySQL 数据目录未复制。生产数据库必须走迁移、备份、恢复或正式导入流程。
- `node_modules` 未复制。目标机器应执行 `npm ci`。
- 前端 `frontend-dist` 可以直接用于静态部署；如果目标机器要重新构建，请使用 `frontend-source`。
- `版本核对.md` 已记录当前 lockfile 锁定版本与 2026-05-15 npm 当前最新版本的差异。
- 历史文档中的 `G:\mutou` 路径请按当前实际路径 `N:\mutou` 或目标服务器路径替换。

## 复制后的基础校验

```powershell
Test-Path N:\mutou\第一版源代码\frontend-source\package.json
Test-Path N:\mutou\第一版源代码\frontend-dist\index.html
Test-Path N:\mutou\第一版源代码\backend-source\package.json
Test-Path N:\mutou\第一版源代码\backend-source\.env.production.example
Test-Path N:\mutou\第一版源代码\docs\03-部署上线\正式上线操作手册.md
```

这些路径都应返回 `True`。
